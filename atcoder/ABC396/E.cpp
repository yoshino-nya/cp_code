// Date: 2025-03-20
// Time: 19:26:24

#include <bits/stdc++.h>
using namespace std;

void ChatGptDeepSeek()
{
    int n, m;
    cin >> n >> m;
    vector<int> x(m), y(m), z(m);
    for (int i = 0; i < m; i++)
        cin >> x[i] >> y[i] >> z[i];
    
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T = 1;
    cin >> T;
    while (T--)
        ChatGptDeepSeek();
    return 0;
}