## C
$\frac{p}{q} <= \frac{2}{3}$

Bob 需要 q 减小， Alice 让 p 减小

$3p <= 2q$

q - 1，相当于 左边 + 2

p - 1, 相当于 左边 - 3

### 3p < 2q 时 Alice 必赢

### 3p > 2q

3p > 2q

## D

$a_i a_j = j - i$

$a_j = \frac{j - i}{a_i} = \frac{j}{a_i} - \frac{i}{a_i}$

$a_i a_j < n$

$a_i < \frac{n}{a_j}$

$i <= a_i a_j - j$

$a_ia_j= 2j - (i + j)$

$(a_i + a_j) ^ 2 = a_i ^ 2 + a_j ^ 2 + 4j - 2(i + j)$

$a_i ^ 2 - 2i + a_j^2 - 2j + 4j$ = $(a_i - 1) ^ 2 + (a_j - 1) ^ 2 + 4j - $

$a_i ^ 2 = i - x$

$x = i - a_i ^ 2$